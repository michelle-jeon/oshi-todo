# 변경 기록

이 문서는 버전별로 실제 반영된 변경 사항을 남긴다. 새 작업은 잠깐 `다음 릴리스 준비 중`에 적을 수 있지만, 버전을 올릴 때는 반드시 해당 버전 섹션으로 옮긴다.

## 다음 릴리스 준비 중

- 아직 없음.

## 0.2.24 - 2026-06-04

- `base_xp_reward` migration이 아직 적용되지 않은 Supabase DB에서도 홈 화면이 완전히 깨지지 않도록 기존 `xp_reward` 기반 fallback을 추가했다.
- 투두/루틴 생성, 수정, XP 조정, 루틴 완료 흐름도 `base_xp_reward` 컬럼이 없을 때 임시로 기존 XP 컬럼만 써서 동작하게 보강했다.
- 스키마 안내 문구가 SQL Editor용 `05_base_xp_rewards.sql` 파일을 직접 알려주게 했다.

## 0.2.23 - 2026-06-04

- 투두/루틴 생성 시 제목을 기준으로 XP를 자동 산정하게 바꾸고, 생성 폼의 수동 XP 입력과 `AI XP` 버튼을 제거했다.
- 투두 제목을 수정하면 XP 기준값과 현재 XP가 새 제목 기준으로 다시 산정되게 했다.
- 투두/루틴의 최초 AI 기준 XP를 `base_xp_reward`로 저장하고, 사용자는 기준값에서 `-10/+10`까지만 조정할 수 있게 했다.
- AI XP 기준값 저장용 Supabase migration을 추가했다.

## 0.2.22 - 2026-06-04

- XP 추천 기준을 5단위가 아니라 1~100 정수 점수로 세밀하게 바꿨다.
- AI 추천 프롬프트에 `책상 위에 휴지 치우기`는 1XP, `관악산 정상 등반하기`는 90XP 같은 기준 예시를 추가했다.
- XP 추천 기본 모델을 `gpt-5-mini`로 바꾸고, 실제 OpenAI API 키를 넣는 방법을 문서에 자세히 추가했다.

## 0.2.21 - 2026-06-04

- XP 추천 호출을 OpenAI Structured Outputs 기반으로 바꿔 응답 형식을 더 안정적으로 만들었다.
- `OPENAI_API_KEY`가 비어 있을 때 임시 추천이 AI 추천처럼 보이지 않도록 안내 문구를 분리했다.
- 임시 추천 규칙에서 등산, 운동, 외출처럼 실제 부담이 있는 짧은 제목을 너무 낮은 XP로 잡지 않게 보정했다.

## 0.2.20 - 2026-06-04

- 투두와 루틴 생성 폼에 AI XP 추천 버튼을 추가했다.
- 투두 생성도 루틴처럼 보상 XP를 설정해 저장할 수 있게 했다.
- OpenAI API 키가 없거나 추천 호출에 실패하면 임시 규칙 기반 XP 추천을 사용하게 했다.
- OpenAI XP 추천용 환경변수를 문서와 `.env.example`에 추가했다.

## 0.2.19 - 2026-06-04

- 개발 브랜치와 운영 브랜치 분리 작업을 백로그에 추가했다.
- AI 기반 투두/루틴 XP 추천과 인플레이션 방지 방향을 백로그에 추가했다.

## 0.2.18 - 2026-06-04

- 첫 캐릭터 생성 화면의 `다른 계정으로 로그인` 버튼을 눈에 덜 띄는 밑줄 버튼으로 낮췄다.
- 루틴 생성과 수정에서 루틴별 XP 보상을 설정할 수 있게 했다.
- 루틴 목록에 각 루틴의 XP 보상을 함께 표시했다.

## 0.2.17 - 2026-06-04

- 캐릭터 `...` 버튼과 상단 계정 메뉴 버튼이 눌리지 않는 드롭다운 상호작용을 안정화했다.
- 캐릭터/계정 메뉴 레이어를 주변 콘텐츠보다 위에 뜨도록 정리했다.

## 0.2.16 - 2026-06-04

- 루틴 종료용 `ends_on` 컬럼이 DB에 아직 없을 때 홈 화면 안내가 4단계 SQL 파일을 직접 알려주게 했다.

## 0.2.15 - 2026-06-03

- Google 로그인 후 홈으로 넘어가는 동안 OshiTodo 스플래시 화면을 보여주게 했다.
- 루틴 시작일을 UTC 오늘이 아니라 사용자가 선택한 날짜로 저장해 전날 캘린더에 생기는 문제를 고쳤다.
- 선택한 날짜부터 루틴을 종료할 수 있게 `ends_on` 흐름을 추가했다.
- 투두 정렬 조회와 루틴 일정 조회에 맞춘 DB 인덱스를 추가했다.
- Supabase SQL Editor에서 바로 실행할 수 있는 루틴 종료/인덱스 SQL 파일을 추가했다.

## 0.2.14 - 2026-06-03

- 작업시간 더미 데이터 SQL에서 `target_user_id_input`으로 대상 profile을 직접 지정할 수 있게 했다.
- 지정한 profile id가 없으면 더 명확한 오류를 보여주게 했다.
- Supabase 설정 문서에 특정 계정으로 더미 데이터를 넣는 방법을 추가했다.

## 0.2.13 - 2026-06-03

- 작업시간 더미 데이터 SQL이 활성 캐릭터가 없어도 최근 캐릭터를 활성화해 진행하게 했다.
- 캐릭터가 아예 없는 profile에는 화면 확인용 더미 캐릭터를 만든 뒤 작업시간 기록을 넣게 했다.
- Supabase 설정 문서에 더미 캐릭터 생성 조건을 설명했다.

## 0.2.12 - 2026-06-03

- Supabase SQL Editor 실행용 작업시간 SQL을 3단계 파일로 분리했다.
- 긴 SQL 복사 중 함수가 잘려 `unterminated dollar-quoted string` 오류가 나는 상황을 줄였다.
- 작업시간 저장 함수에서 캐릭터 row 변수 대신 캐릭터 id만 쓰도록 단순화했다.

## 0.2.11 - 2026-06-03

- Supabase SQL Editor에 바로 붙여넣어 실행할 수 있는 작업시간 통합 SQL을 추가했다.
- 작업시간 로그 테이블, 저장 함수, 조회 인덱스, 더미 기록을 한 번에 준비할 수 있게 했다.
- Supabase 설정 문서에 파일 경로가 아니라 SQL 파일 내용을 붙여넣어야 한다는 안내를 보강했다.

## 0.2.10 - 2026-06-03

- 작업시간 과거 기록을 확인할 수 있는 더미 데이터 SQL을 추가했다.
- 작업시간, XP 기록, 상점, 친구, 광장 조회 경로에 맞춘 DB 인덱스 migration을 추가했다.
- Supabase 설정 문서에 작업시간 더미 데이터와 조회 인덱스 실행 순서를 정리했다.

## 0.2.9 - 2026-06-03

- 작업시간 로그 migration이 아직 적용되지 않았을 때 홈 화면에 DB 스키마 안내가 계속 뜨지 않게 했다.
- 작업시간 로그 저장 함수가 아직 DB에 없으면 홈 사용을 막지 않고 조용히 넘어가게 했다.

## 0.2.8 - 2026-06-02

- 옷장 탭에서 보유 아이템이 없을 때 불필요한 빈 상태 문구가 보이지 않게 정리했다.

## 0.2.7 - 2026-06-02

- 옷장에서 활성 캐릭터의 보유 아이템을 보여주게 했다.
- 구매한 헤어, 눈, 옷, 무늬, 악세서리를 장착해 저장할 수 있게 했다.
- 옷장 기본 아이템과 보유 아이템을 구분해서 표시했다.

## 0.2.6 - 2026-06-02

- 상점, 광장, 캐릭터 방 진입 시 Turbopack 패닉 때문에 서버 렌더 요청이 반복되던 개발 서버 흐름을 webpack 모드로 고정해 안정화했다.
- 세션 쿠키가 없는 보호 페이지 진입은 Supabase 인증 API 호출 전에 로그인 화면으로 이동하게 해 반복 요청이 인증 API 제한으로 번지지 않게 했다.

## 0.2.5 - 2026-06-02

- 캐릭터 목록, 캐릭터 생성, 옷장 화면까지 캐릭터 레이어 이미지를 일반 이미지 렌더링으로 통일했다.
- 남아 있던 Next 이미지 런타임 경로를 제거해 개발 중 이미지 렌더링 깜빡임을 줄였다.

## 0.2.4 - 2026-06-02

- 상점, 광장, 캐릭터 방의 캐릭터 레이어 이미지를 일반 이미지로 렌더링하게 바꿨다.
- 캐릭터 레이어 이미지의 Next 이미지 런타임 반복 반응과 개발 중 깜빡임을 정리했다.

## 0.2.3 - 2026-06-02

- Google 로그인 버튼이 개발 중 반복 초기화되어 라우터 오류와 재렌더링을 유발하던 흐름을 정리했다.
- 로그인 성공 후 Next 라우터 초기화 전 이동 오류가 나지 않도록 이동 방식을 정리했다.

## 0.2.2 - 2026-06-02

- 상점, 광장, 캐릭터 방 이동 중 인증 확인 오류가 로그인 튕김처럼 보이지 않게 했다.
- 상점, 광장, 캐릭터 방에서 사용자 범위를 명확히 제한했다.
- 상점 미리보기의 불필요한 상태 업데이트와 캐릭터 이미지 경고를 줄였다.

## 0.2.1 - 2026-06-02

- 투두와 루틴 완료/취소/삭제에 낙관적 UI를 적용했다.
- 서버 액션 실패 시 이전 UI 상태로 복구하고 화면 안에서 오류 메시지를 보여주게 했다.
- 백로그와 버전 기록을 정리하기 시작했다.

## 0.2.0 - 2026-05-31

- 날짜별 투두 캘린더와 드래그 정렬을 추가했다.
- 해야 할 일과 완료한 일을 섹션으로 분리했다.
- 루틴 생성, 루틴 완료/수정 흐름을 추가하고 루틴 완료가 실제 투두와 XP로 기록되게 바꿨다.
- 친구 검색/팔로우 화면을 추가했다.
- 실시간 광장 MVP와 내 방 화면을 추가했다.
- 캐릭터 생성, 다중 캐릭터 슬롯, 인간 레이어 에셋, 옷장, 상점, 상점 입어보기 흐름을 추가했다.
- 작업 시간 섹션을 프로그램별 활성 시간/XP 리스트로 정리하고, 날짜별 작업시간 기록을 추가했다.
- 계정 설정에서 연결된 로그인 정보를 보여주게 했다.
- XP 획득과 사용 기록 화면을 추가했다.
- 완료한 투두 아카이브 화면을 추가했다.
- 상점 아이템 seed 스크립트와 Supabase migration workflow 문서를 추가했다.
- 배포 환경변수 체크리스트를 정리했다.

## 0.1.0 - 2026-05-21

- OshiTodo 로컬 MVP를 시작했다.
- Supabase 인증, 캐릭터 생성, 투두 CRUD, XP, 옷장, 상점, 작업 XP 실험의 초기 뼈대를 만들었다.
