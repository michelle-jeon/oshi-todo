# Codex 작업 규칙

이 저장소에서 Codex가 작업할 때 지켜야 하는 프로젝트 규칙입니다.

## 언어

- `docs`, `CHANGELOG.md`, `docs/VERSIONING.md`, 백로그 문서는 한국어로 작성한다.
- 사용자에게 설명하는 변경 내용도 한국어로 작성한다.

## 커밋 메시지

- 커밋 메시지는 `feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `test:` 같은 접두사를 붙인다.
- 접두사 뒤의 설명은 한국어로 쓴다.
- 예시:
  - `feat: 옷장 인벤토리 장착 기능 추가`
  - `fix: 보호 페이지 반복 렌더링 방지`
  - `docs: 배포 환경변수 체크리스트 정리`

## 백로그와 버저닝

- 백로그 항목을 해결하면 항목 앞에 `✅`를 붙이고, 해결 방식은 한국어로 짧게 남긴다.
- 작업 중 새로 발견한 후속 작업은 `docs/BACKLOG.md`에 한국어로 추가한다.
- 기능 또는 버그 수정이 끝나면 `package.json` 버전을 올리고 `docs/VERSIONING.md`와 `CHANGELOG.md`를 갱신한다.
- `CHANGELOG.md`는 `버전 - 날짜` 형식으로 정리한다. 완료된 변경을 `다음 릴리스 준비 중` 아래에 계속 쌓아두지 않는다.
